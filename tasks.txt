In addition to making the following changes to the files generated by applying the specification in hsqldb.txt to a
model like that stored in model.xml, make the same meta-changes to hsqldb.txt, CLAUDE.md and DESIGN.md so that these
changes below will be effective for all future code creation

1) In model.html, replace the table of maven artifact attributes with the actual dependency stanza a user would have
   to add to their application's pom.xml file in order to use the API's in this artifact.
   
2) In model.html, sort the method names alphabetically    

2) For each of the classes created, add javadoc-style comments for each class, each attribute and each method. You can
   use the descriptions found in model.html